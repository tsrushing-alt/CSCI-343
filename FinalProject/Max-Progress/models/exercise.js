export default class Exercise {
  constructor(id, name, muscleGroup) {
    this.id = id;                 // unique string or number
    this.name = name;             // e.g., "Bench Press"
    this.muscleGroup = muscleGroup; // e.g., "Chest"
  }
}
